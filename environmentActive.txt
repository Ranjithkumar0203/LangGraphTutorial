do below inside python folder
rm -rf venv
python -m venv venv
source venv/Scripts/activate

pip install -r requirements.txt

python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000

ng serve --host 0.0.0.0 --port 4200